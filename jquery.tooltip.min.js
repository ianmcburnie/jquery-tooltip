
(function($){$.fn.tooltip=function(options){options=$.extend({hideOnScroll:true,hideOnResize:true,alignX:'baseline',alignY:'bottom',offsetTop:0,offsetLeft:0},options);return this.each(function onEach(){var $tooltipWidget=$(this);var $focusable=$tooltipWidget.focusable().first();var $overlay=$('#'+$focusable.attr('aria-describedby'));var $window=$(window);function stick(){return $overlay.stick($tooltipWidget,options);}
function show(){if($overlay.attr('aria-hidden')!=='false'){stick();$overlay.attr('aria-hidden','false');if(options.hideOnScroll===true){$window.one('scroll',hide);}else{$window.on('scroll',stick);}
if(options.hideOnResize===true){$window.one('resize',hide);}else{$window.on('resize',stick);}
$tooltipWidget.trigger('tooltipShow');}}
function hide(){if($overlay.attr('aria-hidden')==='false'){$window.off('scroll resize',stick);$window.off('scroll resize',hide);$overlay.attr('aria-hidden','true');$tooltipWidget.trigger('tooltipHide');}}
$overlay.insertAfter($focusable);$focusable.not('input').on('mouseenter',show);$tooltipWidget.not('.tooltip--input').mouseExit().on('mouseExit',hide);$focusable.on('focus',show);$tooltipWidget.focusExit().on('focusExit',hide);$tooltipWidget.addClass('tooltip--js');});};}(jQuery));